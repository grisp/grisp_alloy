# Hello Elixir (with NIF)

Minimal Elixir application demonstrating a NIF built via `elixir_make` and exercised on boot.

Build steps are handled by GRiSP ALLoy `build-firmware.sh` using the Elixir project plugin.
