# Task Context: Task 5.6a - Buildroot hook wrapper implementation

## Objective
Implement `scripts/buildroot/script_hook.sh` as the real Buildroot hook wrapper for post-build/post-image/post-fakeroot dispatch using generated context arrays.

## Scope
- Implement wrapper dispatch behavior and failure semantics.
- Source the same common utility base used by command scripts (`scripts/utils/common.sh`).
- Add focused wrapper tests for hook selection and error propagation.

## Out of Scope
- Firmware hook orchestration (`Task 7.6`).
