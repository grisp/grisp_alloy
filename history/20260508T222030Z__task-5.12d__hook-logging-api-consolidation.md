# Task Context: Task 5.12d - Hook logging API consolidation and nugget hook migration

## Started
- Timestamp (UTC): 2026-05-08T22:20:30Z
- Planning entry: docs/PLANNING.md Task 5.12d.

## Objective
Consolidate hook-facing logging behind a shared API sourced via `hook_common.sh`, migrate builtin nugget hooks to that API, add tests for standardized hook logging/path formatting behavior, and update design/workflow docs accordingly.

## Outcome
- Added `scripts/utils/debug_tools.sh` as the canonical hook logging module with:
  - `alloy_log_error|warn|info|debug`
  - `alloy_die`, `alloy_enter_hidden`, `alloy_leave_hidden`
  - `alloy_log_format_path`
  - deterministic hook-context log prefixing (`[<hook_type>:<nugget>]`).
- Refactored `scripts/utils/hook_common.sh` to source `debug_tools.sh` and keep hook entry-point responsibilities (trace + conditional tool loading).
- Migrated `scripts/utils/sdk_tools.sh` from legacy `alloy_hook_*` calls to shared `alloy_log_*` / `alloy_die` and path-format logging.
- Migrated builtin hook `nuggets/builder_buildroot/scripts/pre-build.sh` to source `hook_common.sh` and use shared hook logging/path formatting.
- Updated docs:
  - `docs/03_ALLOY_DESIGN.md` (hook logging/path helper contract)
  - `docs/WORKFLOW.md` (require shared hook logging API usage)
  - `CHANGELOG.md` (user/developer-visible behavior change)

## Tests
- Baseline gate: `./scripts/tests/gates/baseline.sh` (pass)
- Full gate: `./scripts/tests/gates/full.sh` (pass)
- Added `scripts/tests/test_debug_tools.sh` and updated existing hook/nugget tests to include the new hook logging module in fixtures and direct hook invocation setup.

## Notes
- Confirmed and preserved the boundary contract: `scripts/buildroot/script_hook.sh` is orchestrator-side (sources `common.sh`), while hook scripts source `hook_common.sh` and consume `*_tools.sh` APIs.
