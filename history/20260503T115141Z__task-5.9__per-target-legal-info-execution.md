# Task Context: Task 5.9 Per-target legal-info execution

- Task ID: 5.9
- Repository: grisp_alloy
- Started (UTC): 2026-05-03T11:51:41Z
- Completed (UTC): 2026-05-03T11:55:48Z
- Status: DONE

## Goal
Implement per-target `make legal-info` execution in `alloy build sdk` so each target workspace produces legal-info output for later merged manifest/legal consolidation.

## Design References
- `docs/03_ALLOY_DESIGN.md` §5.5 SDK build flow step 10
- `docs/01_DATA_DESIGN.md` SDK build directory (`targets/<TARGET_ID>/workspace/legal-info`)

## Implemented Changes
1. Added `build_sdk_run_target_legal_info` in `scripts/commands/build-sdk.sh`.
2. Wired a dedicated legal-info loop after successful per-target Buildroot build execution, preserving deterministic target order from `ALLOY_PLAN_TARGET_IDS`.
3. Kept legal-info execution context aligned with build execution (`-C`, `O=`, `BR2_EXTERNAL`, `ALLOY_*`, debug-to-`V=1` behavior).
4. Updated command summary hint to reflect that per-target legal-info now runs in Task 5.9.
5. Expanded integration coverage in `scripts/tests/test_build_sdk_command.sh` to assert:
   - one `legal-info` invocation per target,
   - target-local context isolation,
   - legal-info execution ordering after per-target build completion.

## Validation
- Baseline gate before edits: `./scripts/tests/gates/baseline.sh` (PASS)
- Focused validation:
  - `./scripts/tests/test_build_sdk_command.sh` (PASS)
  - `./scripts/tests/check_shell_syntax.sh` (PASS)
  - `./scripts/tests/check_shell_lint.sh` (PASS; existing non-blocking warnings in untouched `build-firmware.sh`)
- Full gate before finalization: `./scripts/tests/gates/full.sh` (PASS)

## Follow-up Notes
- Task 5.10 remains the next pending SDK-flow step (auxiliary sdk output collection/validation).
- Task 5.11 remains responsible for the main-target Smelterl legal/manifest consolidation pass.
