# Task Context: Task 5.10 Auxiliary sdk output collection and validation

- Task ID: 5.10
- Repository: grisp_alloy
- Started (UTC): 2026-05-03T16:31:46Z
- Completed (UTC): 2026-05-03T17:53:55Z
- Status: DONE

## Goal
Collect auxiliary `.sdk_outputs` registrations, validate their produced paths, stage outputs under SDK staging, and prepare deterministic `ALLOY_SDK_OUTPUT_*` mappings for later main-target consumption.

## Reopened Scope Note
Task 5.10 was reopened to complete the missing producer-side contract:
- implement hook-side sdk output registration entry point/API (`hook_common.sh` + `sdk_tools.sh`) so hooks can write `.sdk_outputs` via `alloy_sdk_add_output`.

## Design References
- `docs/03_ALLOY_DESIGN.md` §5.5 step 11
- `docs/03_ALLOY_DESIGN.md` §8.6.0 `hook_common.sh` and §8.6.17 `sdk_tools.sh`
- `docs/01_DATA_DESIGN.md` sdk output registration/mapping contract
- `smelterl/docs/DESIGN.md` main-context orchestrator-provided sdk output consumption variables

## Implemented Changes
1. Implemented orchestrator-side Task 5.10 flow in `scripts/commands/build-sdk.sh`:
   - auxiliary-target filtering,
   - declared output id loading from `ALLOY_SDK_OUTPUTS`,
   - strict `.sdk_outputs` registry/path validation,
   - deterministic staging into `staging/auxiliary/<AUX_ID>/outputs/<OUTPUT_ID>/...`,
   - deterministic export mappings (`ALLOY_SDK_OUTPUT_<AUX_ID>_<OUTPUT_ID>` always, unique-only `ALLOY_SDK_OUTPUT_<OUTPUT_ID>`).
   - then extracted this orchestrator-side implementation into `scripts/utils/sdk_utils.sh` so `build-sdk.sh` consumes utils-side helpers and the utils/tools boundary matches design intent.
2. Injected orchestrator-resolved auxiliary output mappings into main `alloy_context.sh` for later SDK/firmware-time consumers.
3. Added hook-side producer API files:
   - `scripts/utils/hook_common.sh` (hook entry point; conditional SDK-time tool loading),
   - `scripts/utils/sdk_tools.sh` (`alloy_sdk_add_output`, `alloy_sdk_has_output`, `alloy_sdk_get_output`).
4. Extended tests:
   - `scripts/tests/test_build_sdk_command.sh` for valid/missing/duplicate collection behavior,
   - `scripts/tests/test_buildroot_script_hook.sh` fixture copies and end-to-end hook test that sources `hook_common.sh` and registers `.sdk_outputs` via `alloy_sdk_add_output`.

## Validation
- Baseline before edits: `./scripts/tests/gates/baseline.sh` (PASS)
- Focused validation:
  - `./scripts/tests/test_build_sdk_command.sh` (PASS)
  - `./scripts/tests/test_buildroot_script_hook.sh` (PASS)
  - `./scripts/tests/check_shell_syntax.sh` (PASS)
  - `./scripts/tests/check_shell_lint.sh` (PASS; existing non-blocking warnings in untouched `build-firmware.sh`)
- Full gate before finalization: `./scripts/tests/gates/full.sh` (PASS, including Smelterl suites)

## Follow-up Notes
- Next pending SDK flow task is Task 5.11 (main legal/manifest consolidation pass).
