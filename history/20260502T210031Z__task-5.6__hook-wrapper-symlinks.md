# Task Context: Task 5.6 - Hook wrapper symlinks and target context symlink

## Objective
Implement Task 5.6 by wiring per-target Buildroot hook-wrapper symlinks and context symlink in the generated target layout, and add a minimal wrapper script so symlink targets resolve.

## Scope and Boundaries
- Repository: `grisp_alloy`
- In scope:
  - `scripts/commands/build-sdk.sh` target-layout symlink wiring
  - `scripts/buildroot/script_hook.sh` minimal placeholder wrapper
  - tests for expected symlink layout
  - planning update to track later full wrapper behavior
- Out of scope:
  - full hook dispatch semantics (tracked as Task 5.6a)

## Design Notes
- Keep the target layout created in Task 5.5 unchanged and add links in place.
- Use relative symlinks from target-local board scripts directory to keep paths portable within the workspace.
- Minimal wrapper must fail clearly when invoked to avoid silent behavior.
